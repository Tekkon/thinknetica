- unless votable.vote(current_user).nil?
  = votable.vote_description(current_user)
  br
  = link_to 'Revote', votable.class.to_s == 'Question' ? revote_question_path(votable) : revote_answer_path(votable), class: 'revote-link', data: { remote: true, method: :post }
