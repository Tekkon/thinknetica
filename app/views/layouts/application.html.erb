<!DOCTYPE html>
<html>
  <head>
    <title>Thinknetica</title>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <% if user_signed_in? %>
      <a href="/logout">Sign out</a>
    <% end %>

    <%= yield %>
  </body>
</html>
