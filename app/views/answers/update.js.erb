$('#answer-<%= @answer.id %>').html('<%= j render @answer %>');
//$('.errors').html('<%= j render 'shared/error_messages', target: @answer %>');
$('.edit-answer-link').click(function(e) {
    e.preventDefault();
    $(this).hide();
    answer_id = $(this).data('answerId');
    $('form#edit-answer-' + answer_id).show();
});
